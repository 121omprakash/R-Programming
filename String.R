name <-'om Prakash'
# grep('pattern',variable) --> return 1 if pattern is found else return 0 if pattern is not found in variable
grep('Om',name)

grep('a',name)
grep('om',name)
#nchar(var) --> returns the no of character in string or variable
cat('No of character in name: ',nchar(name))

#string concatenation --> adding two strings and creating a single string
print(paste('Hello','World')) 
# output --> Hello world